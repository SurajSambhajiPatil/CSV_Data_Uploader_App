Objective: a microservice that handles the upload of CSV files, processes the data 
(including extraction and transformation), and loads the data into a PostgreSQL database. 
Ensure the microservice is secure and well-tested.
